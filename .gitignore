# Allow list: every top-level entry is ignored unless listed below.
# Each exercise directory keeps its own allow list for its contents.
/*
!/.gitignore
!/.envrc
!/.devcontainer/
!/.vscode/
!/Makefile
!/README.md
!/tools/
!/ex0[0-9]/
